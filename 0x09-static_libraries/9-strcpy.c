#include "main.h"
#include <string.h>
/**
 * _strcpy - copy string
 * @dest: string destination
 * @src: string to copy
 *
 * Return: return string dest
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
