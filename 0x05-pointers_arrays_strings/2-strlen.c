#include "main.h"
#include <string.h>
/**
 * _strlen(char *s) - calculate the length of string
 * @s: string variable
 *
 * Return: return the length of the string
 */
int _strlen(char *s)
{
	int len = 0;

	len = strlen(s);
	return (len);
}
